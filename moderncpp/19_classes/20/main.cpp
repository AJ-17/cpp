/*
Sometimes instead of using inheritence we use composition [these is when we create the memebers in child class one of the member is have a datatype of parent class] it helps in increasing the lifetime of  base class properties as we can just provide a reference in the child class even if child class go out of scope still base class will be there

*/